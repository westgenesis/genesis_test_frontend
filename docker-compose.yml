services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: genesis-test-frontend
    ports:
      - "8077:80" # 本地 8077 → 容器 nginx 80
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
