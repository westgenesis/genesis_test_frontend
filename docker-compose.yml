services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: genesis-test-frontend
    ports:
      - "8088:80" # 本地 8088 → 容器 nginx 80
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
